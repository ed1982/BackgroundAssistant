# Runtime dependencies (Python 3.10–3.12, macOS + Windows)
faster-whisper>=1.0.3      # local speech-to-text (CTranslate2)
sounddevice>=0.4.6         # microphone capture (PortAudio, bundled in wheels)
# webrtcvad-wheels: maintained fork of webrtcvad with prebuilt wheels for all
# platforms (the original needs a C build and breaks on Python 3.12).
webrtcvad-wheels>=2.0.14     # voice activity detection / endpointing
PySide6>=6.6               # system-tray background UI
pyttsx3>=2.90              # TTS on Windows (SAPI5); macOS default is `say`
